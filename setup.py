from setuptools import setup

setup(
   name='polycomp',
   version='0.0.1',
   description='Python module using cupy for polymer field theoretic simulation',
   author='Emmit Pert',
   author_email='epert@stanford.edu',
   packages=['polycomp'],  #same as name
   install_requires=[], #external packages as dependencies
)